# UrbanSense integration tests

Run the smoke test from the repository root:

```bash
python tests/integration_smoke.py
```

The test validates the shared event schema, pipeline construction, and multi-fleet road-event fusion without requiring model weights.
