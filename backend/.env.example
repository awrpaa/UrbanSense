# Copy to .env locally. Never commit real credentials.
TOMTOM_API_KEY=

# Optional production database
DATABASE_URL=

# Set to true only when the real model adapters are installed/configured.
URBANSENSE_MODELS_ENABLED=false
